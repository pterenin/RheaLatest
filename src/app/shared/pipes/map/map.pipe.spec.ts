import { ValueByKeyMapPipe } from './map.pipe';

describe('ValueByKeyMapPipe', () => {
  it('Create an instance', () => {
    const pipe = new ValueByKeyMapPipe();
    expect(pipe).toBeTruthy();
  });
});
