import { TableSearchFilterPipe } from './table-search-filter.pipe';

describe('Table search filter pipe tests', () => {
  describe('TableSearchFilterPipe', () => {
    it('create an instance', () => {
      const pipe = new TableSearchFilterPipe();
      expect(pipe).toBeTruthy();
    });
  });
});
