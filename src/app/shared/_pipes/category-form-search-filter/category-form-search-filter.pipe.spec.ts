import { CategoryFormSearchFilterByProperties } from './category-form-search-filter.pipe';

describe('Category form search filter by properties tests', () => {
  describe('CategoryFormSearchFilterByProperties', () => {
    it('create an instance', () => {
      const pipe = new CategoryFormSearchFilterByProperties();
      expect(pipe).toBeTruthy();
    });
  });
});
